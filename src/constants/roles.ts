import { PORT } from "./env";

export const STUDENT = "estudiante";
export const TEACHER = "profesor";
export const OTHER = "otro";
export const SERVER_ON = `Server running in http://localhost:${PORT}/`;
